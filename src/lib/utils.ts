// Lightweight compatibility shim
// Re-export helpers from the existing utils so older import paths continue to work.
export { cn } from "@/utils/tailwind-helpers";

// You can add other shared helpers here later (e.g., formatters, validators).
